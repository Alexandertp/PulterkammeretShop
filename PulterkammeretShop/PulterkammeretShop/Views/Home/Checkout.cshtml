@{
    ViewData["Title"] = "Checkout";
}
@model List<Spil>

<div class="text-center">
    @if (Model != null)
    {
        double totalPris = 0;
        @foreach (Spil spil in Model)
        {
            totalPris += spil.pris;
            
            <div>
                @spil.navn
            </div>
        }   
        <div>
            Fuld pris: @totalPris
        </div>
    }
</div>
