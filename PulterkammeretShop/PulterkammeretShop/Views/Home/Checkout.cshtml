@{
    ViewData["Title"] = "Checkout";
}
@model List<Spil>

<div class="text-center">
    @if (Model != null)
    {
        @foreach (Spil spil in Model)
        {
            <div>
                @spil.navn
            </div>
        }   
    }
</div>
